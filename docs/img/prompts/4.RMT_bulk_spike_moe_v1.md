# Bulk/spike RMT mixture of experts, v1

- `status`: approved_v2
- `representation_type`: intrinsic spectral geometry and AI-native mechanism
- `visual_primitives`: singular-value spectrum, empirical bulk edge, row-to-component links, leverage cap, soft gate, covariance ellipses, expert mixture
- `analogy_family`: none
- `novelty_check`: separates component-level spectral spikes from row-level leverage before introducing a hierarchical expert topology
- `failure_mode`: high leverage is mistaken for a spike; unrelated spike directions are sent to one expert; an analytic bulk edge is used outside its assumptions
- `output_v1`: `docs/img/4.RMT_bulk_spike_moe_v1.png`
- `output_v2`: `docs/img/4.RMT_bulk_spike_moe_v2.png` (corrected projection of a new unfolding)

## Generation prompt

> ICML-style scientific figure, clean academic vector infographic, white background, muted blue-gray palette with one accent color, minimal typography, precise arrows, thin lines, labeled panels, no photorealism, no 3D glossy rendering, no decorative background, conference-paper figure aesthetics, mathematically clean, visually balanced. Russian-first scientific labels; use English only for standard abbreviations. Clearly distinguish established result, analogy, research hypothesis, and failure mode.
>
> Create a wide 16:9 figure titled "От спектрального bulk к иерархическому ансамблю". Do not include benchmark results or performance claims.
>
> Panel A, "Спектральная декомпозиция": show `M = S + N`, an empirical null bulk, an edge `sigma_hat_plus`, and separated spike components. Include `J_spike = {j : sigma_j > sigma_hat_plus}` and state that spikes belong to spectral directions.
>
> Panel B, "Участие строк в компонентах": show that every row can participate in both bulk and spike directions. Include row leverage, spike excess energy, and soft signalness. Explicitly contrast `l_i > Q_0.95` with `sigma_j > sigma_hat_plus`; the leverage cap limits row influence and is not a spectral bulk threshold.
>
> Panel C, "Иерархический mixture of experts": show one regularized bulk expert, several experts for distinct spike signatures, a soft signal/bulk gate, and Mahalanobis or GMM routing among spike experts. Include `P(y|x) = (1-g(x)) P_bulk(y|x) + g(x) sum_k r_k(x) P_spike,k(y|x)`.
>
> The bottom strip must state: high leverage does not prove a spike; one expert for all high-leverage rows mixes distinct directions; tabular contractions require an empirical null edge. Mark the distinction between spectrum and leverage as established, and the bulk-plus-spike expert topology as a research hypothesis.

## V2 edit prompt

> Preserve the figure and replace only the incorrect new-sample projection `z = U^T x` with `z(x) = M_new V_r`, consistent with `embedding_mode="sv_scaled"`.
